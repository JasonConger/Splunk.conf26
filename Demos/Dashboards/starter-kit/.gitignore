# Secrets — never commit these
.env
.env.*
*.env
*.token
*token*.txt
.mcp.json.local

# Claude Code per-user settings (may hold approved commands and paths)
.claude/settings.local.json

# Build output
dist/
node_modules/
